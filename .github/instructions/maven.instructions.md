---
description: "用于编辑 Maven 构建文件或依赖配置。覆盖依赖管理、插件范围和可复现构建。"
applyTo: "**/pom.xml"
---
# Maven 构建规范

- 新增依赖要尽量少，并且理由充分。
- 优先使用受管理版本和一致的 Java 版本目标。
- 保持构建可复现，并兼容 CI。
- 只有在插件确实支持当前任务时才更新插件。
- 避免与当前需求无关的版本漂移。
- 如果构建变更会影响测试或打包，要在同一次任务中同步更新工作流和验证步骤。
- 新增依赖时，要说明为什么需要它，以及是否考虑过更轻量的替代方案。
