name: Java 鎸佺画闆嗘垚

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 妫€鍑轰粨搴?
        uses: actions/checkout@v4

      - name: 閰嶇疆 JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: 杩愯 Maven 娴嬭瘯
        if: hashFiles('backend/pom.xml') != ''
        run: mvn -B -ntp -f backend/pom.xml test

      - name: 鐩墠杩樻病鏈?Maven 椤圭洰
        if: hashFiles('backend/pom.xml') == ''
        run: echo "褰撳墠杩樻病鏈?backend/pom.xml锛涜鍏堟坊鍔?Maven 椤圭洰浠ュ惎鐢?CI銆?

