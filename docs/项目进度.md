# 项目进度

> 用于记录当前项目的真实状态。建议在每个任务完成、每次 bug 修复完成、或阶段切换后更新这里。

## 1. 项目概览

- 项目名称：个人程序员知识付费社区 MVP
- 当前阶段：后端脚手架已落地，准备进入第一轮业务开发
- 当前目标：在 `backend/` 下完成社区 MVP 后端工程底座，并基于该底座拆分第一批业务模块
- 负责人：个人开发者
- 最近更新时间：2026-04-09

## 2. 已完成里程碑

| 日期 | 任务 | 结果 | 验证 |
|------|------|------|------|
| 2026-04-08 | 建立开发规范、需求模板、bug 模板、收尾模板 | 已完成 | 文档检查 |
| 2026-04-08 | 建立 DDD / Flyway / 前端 instruction | 已完成 | 规则文件检查 |
| 2026-04-08 | 建立 MCP / hooks 指南与示例配置 | 已完成 | 配置与脚本检查 |
| 2026-04-08 | 建立 MCP 统一服务入口脚本 | 已完成 | 代码检查 |
| 2026-04-08 | 建立 PreToolUse 危险操作拦截脚本 | 已完成 | 代码检查 |
| 2026-04-08 | 建立真实的 bug / feature 记录目录 | 已完成 | 目录与文件检查 |
| 2026-04-09 | 在 `backend/` 下建立 Spring Boot 后端脚手架 | 已完成 | Maven 替代性测试通过 |

## 3. 当前进行中

- Copilot / Codex 客户端的 MCP 注册配置还未真正填入本地环境。
- 当前机器默认 JDK 仍是 17，项目基线 Java 21 的完整验证待补。
- 社区 MVP 的第一轮业务模块开发尚未开始。

## 4. 待办与计划

- 按需求文档拆分 MVP 第一阶段功能。
- 完成 Copilot / Codex 的本地 MCP 配置。
- 在 Java 21 环境下重新执行完整构建与测试。
- 开始功能开发后，为每个里程碑补充 feature 记录。

## 5. 风险与阻塞

- 客户端配置格式因 Copilot / Codex 版本不同可能需要本地微调。
- GitHub Token、仓库名、工作区路径等实际值需要在本地填写。
- 当前机器默认 JDK 为 17，项目的 Java 21 基线还没有在本机完成完整验证。

## 6. 最近验证

- 最近运行的测试：`mvn -B -ntp -s backend/settings.xml -f backend/pom.xml "-Djava.version=17" test`
- 测试结果：`backend` 脚手架的 5 条测试全部通过；本机默认 JDK 17 下完成了替代性验证。
- 是否有未覆盖场景：Java 21 基线下的完整本机构建验证、MCP 真连接、hooks 真触发、Copilot / Codex 客户端注册。

## 7. 最近收尾记录

- 最近完成的任务：`backend` 后端脚手架初始化
- 完成时间：2026-04-09
- 主要变更：新增 Spring Boot 主程序、Maven 构建、DDD 分层目录、统一返回体、异常处理、示例接口、安全骨架、Flyway 基线脚本、测试和 backend README
- 验证方式：Maven 替代性测试、文档检查
- 剩余风险：Java 21 本机验证、客户端接入、第一轮业务模块尚未落地

## 8. 下一步建议

- 先在 Java 21 环境下补跑一次 `backend` 的完整构建与测试。
- 再按需求文档拆出第一个业务模块。
- 同步把 Copilot / Codex 的 MCP 真正配进去。

## 9. 关联记录

- [bug 修复记录目录](bug-fix/README.md)
- [feature 开发记录目录](feature-development/README.md)
- [MCP / hooks 配置指南](mcp-hooks-setup.md)

## 10. bugfix 收口记录

- 2026-04-09 完成 `backend` 脚手架 bugfix 修复，补齐 JWT 认证态、健康探针白名单、请求体解析错误处理和 Maven 本地仓库示例可移植性。
- 验证结果：`mvn -B -ntp -s backend/settings.xml -f backend/pom.xml "-Djava.version=17" test` 通过。
